import React from 'react';
import HomePageContent from '../components/HomePage'; // Assuming this is the correct component to render

const HomePage = () => {
    return <HomePageContent />;
};

export default HomePage;
